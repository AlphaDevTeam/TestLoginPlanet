import 'package:flutter/material.dart';
import 'FancyRow.dart';

class DataListFancy extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
      return new FancyRow(null);
    }
}